import React from 'react'
import {Stack, Box, Typography} from '@mui/material'

interface SerialPortInputOptions{
    name: string,
}

export default function SerialPortInput(props : SerialPortInputOptions){
    return ( 
    <Stack spacing = {3}>
    </Stack>);
}